<img src="banner.svg"></img>

Quick and simple pace tracker for the web. Uses the browser's `localStorage` to store timestamps and computes the average pace.

This was mostly generated code, so don't expect anything super quality. Mainly for fun, to try out Gemini. I'll probably do some manual updating with different features later.